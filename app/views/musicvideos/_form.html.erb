<%= form_for(@musicvideo) do |f| %>
  <% if @musicvideo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@musicvideo.errors.count, "error") %> prohibited this musicvideo from being saved:</h2>

      <ul>
      <% @musicvideo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :artist %><br>
    <%= f.text_field :artist %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
